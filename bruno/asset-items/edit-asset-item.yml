info:
    name: edit-asset-item
    type: http
    seq: 6

http:
    method: PATCH
    url: "{{baseUrl}}/api/asset-items/019e78e1-e4bc-7d8b-b8a1-5a158d212bd9"
    body:
        type: json
        data: |-
            {
              "name": "Paasa Broker Account"
            }
    auth:
        type: bearer
        token: "{{accessToken}}"

settings:
    encodeUrl: true
    timeout: 0
    followRedirects: true
    maxRedirects: 5

